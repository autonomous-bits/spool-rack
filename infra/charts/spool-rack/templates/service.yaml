apiVersion: v1
kind: Service
metadata:
  name: {{ include "spool-rack.fullname" . }}
  labels:
    {{- include "spool-rack.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "spool-rack.selectorLabels" . | nindent 4 }}
